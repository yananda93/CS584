I used opencv to load the weights and configuration to a darknet model.
To run the script, the model weight, cfg file, the VOC dataset(TrainVal folder) and the script need to be placed under the same folder.
The weights and cfg files are downloaded from: https://pjreddie.com/darknet/yolov2/